# Qwen2-VL SLM Defect Detection - Requirements
# Python 3.9+ recommended

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0
pillow>=10.0.0

# Transformers and PEFT
transformers>=4.40.0
peft>=0.11.0
accelerate>=0.30.0
bitsandbytes>=0.43.0

# Training utilities
tensorboard>=2.15.0
wandb>=0.17.0  # Optional, for experiment tracking

# Metrics and evaluation
scikit-learn>=1.4.0
scipy>=1.12.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Data processing
tqdm>=4.66.0
python-dateutil>=2.9.0

# Optional but recommended
# flash-attn>=2.5.0  # For faster attention (requires CUDA, install separately)
# qwen-vl-utils>=0.0.8  # Qwen2-VL specific utilities

# Development
pytest>=8.0.0  # For testing
black>=24.0.0  # For code formatting
flake8>=7.0.0  # For linting
