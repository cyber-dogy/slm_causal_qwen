# -*- coding: utf-8 -*-
import io
import sys

sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')

with open('Casual-Qwen_Paper_CN.tex', 'r', encoding='utf-8') as f:
    content = f.read()

# 替换整个引言部分
old_intro = '''\\section{引言}

选择性激光熔化（Selective Laser Melting, SLM）是一种重要的金属增材制造技术，通过逐层熔化金属粉末来构建复杂几何形状的零件 \\cite{ref_pbf_review}。该技术能够成形传统工艺难以实现的复杂内部结构和定制化组件，已广泛应用于航空航天、医疗器械和模具制造等领域 \\cite{ref_optical_defect}。然而，SLM 成形过程中涉及复杂的物理现象，包括熔池动力学、热传导和凝固行为，这些因素的波动会直接影响最终零件的缺陷形成 \\cite{ref_ir_defect}。即使是微小的工艺偏差也可能导致气孔、裂纹和几何变形等缺陷，严重影响零件的机械性能和服役可靠性，造成显著的经济损失。

为实现 SLM 过程的在线监测与早期预警，工业系统通常部署多种传感器同步采集过程数据。其中，可见光成像能够捕捉熔池形态和表面纹理特征，而红外热成像则可反映热输入分布和温度场演化 \\cite{ref_multisensor_am,ref_multisensor_platform}。这种多模态观测方式提供了互补的缺陷表征信息，有助于提升异常检测的准确性和鲁棒性。然而，现有的基于单一传感器或简单多模态融合的监测方法通常假设训练数据与测试数据来自相同的分布，在实际工业部署中面临严峻挑战：当遇到未见过的工艺参数、设备状态或材料批次时，模型的识别性能往往会显著下降。

然而，现有深度学习方法大多假设训练数据与测试数据来自相同分布，这一假设在工业场景中难以成立。当模型部署于训练时未见的工况（unseen condition）时——如不同的激光功率、扫描速度、设备型号或粉末批次——其识别性能往往显著退化。Arjovsky 等 \\cite{ref_irm} 指出，此类失败源于模型对训练数据中虚假相关性（spurious correlations）的过度依赖，如背景纹理、光照条件或设备噪声等表面统计特征，而非缺陷本质的因果特征。在 SLM 多模态监测中，这一问题表现为：模型可能将特定工况下的热背景模式或设备噪声误识为缺陷依据，导致在未见工况下失效。因此，如何实现训练工况到未见工况的稳定识别，而非简单记忆训练环境的统计规律，成为 SLM 多模态缺陷识别的核心挑战。

近年来，大规模视觉语言模型（Vision-Language Models, VLMs）如 Qwen2-VL 展现出强大的多模态表征学习能力 \\cite{ref_qwen2vl}，为未见工况下的 SLM 缺陷识别提供了潜在的技术路径。一方面，这些模型在海量多模态数据上预训练，具备处理可见光与红外等多源视觉输入的能力；另一方面，其预训练过程中接触到的多样化视觉模式，理论上应有助于提取跨工况稳定的特征。然而，直接将 VLM 应用于工业小样本场景面临适配难题：现有实验表明，简单的提示工程（prompt engineering）方法在未见工况设定下性能显著不足。这表明大模型的多模态先验并不能自动转化为对工业缺陷的准确识别，关键在于如何设计针对性的适配机制，使其在保持跨工况稳定性的同时，有效融合 SLM 多模态监测数据中的互补信息。

本文提出一种基于大模型先验与因果稳定约束的未见工况缺陷识别方法。核心思路是将预训练视觉大模型作为跨工况稳定的特征提取器，通过显式多模态融合头整合异构传感器数据（可见光与红外热信息），并在融合表示空间中施加因果一致性约束。该方法不依赖于冻结主干网络的简单提示学习，而是通过轻量化的判别式适配机制，在保留大模型预训练先验的同时，学习跨工况不变的缺陷表征。这里的"因果稳定"概念源于如下假设：与工况相关的纹理、亮度和热背景特征会随工艺条件变化而变化，而缺陷本体的本质特征应当在跨工况时保持更稳定的判别作用。通过对同标签但来自不同工况的样本施加一致性约束，模型被迫减少对不稳定环境因素的依赖，从而学习到从训练工况到未见工况的稳健映射。

围绕上述思路，本文开展三方面工作：（1）构建条件级主协议与外部迁移测试集，建立未见工况下模型性能的系统评估框架；（2）设计大模型先验引导的判别式适配方法，通过因果稳定约束增强跨工况泛化能力；（3）分析可见光与红外模态在未见工况下的互补机制，探索多模态信息的最优融合策略。
\\begin{enumerate}
\\item 构建了面向 SLM 缺陷识别的"条件级主协议 + 外部迁移压力测试"评估框架，明确了小样本、未见工况场景下的模型性能边界，为工业视觉模型的实际部署提供参考。
\\item 提出一种大模型轻量适配框架，将预训练视觉大模型的表征能力与显式多模态融合相结合，并通过因果稳定约束增强未见工况下的识别性能，在条件级主协议和外部迁移测试上均取得最优表现。
\\item 通过系统的对照实验揭示：在当前小样本工业场景中，保留大模型预训练先验并结合因果稳定约束，比激进地端到端微调视觉主干更有利于未见工况下的鲁棒识别。
\\end{enumerate}'''

new_intro = '''\\section{引言}

选择性激光熔化（Selective Laser Melting, SLM）通过逐层熔化金属粉末构建复杂几何零件，已广泛应用于航空航天、医疗器械和模具制造等领域 \\cite{ref_pbf_review,ref_optical_defect}。SLM 成形涉及熔池动力学、热传导和凝固等复杂物理过程，工艺参数的波动会直接导致气孔、裂纹和几何变形等缺陷 \\cite{ref_ir_defect}。微小的工艺偏差即可显著影响零件的机械性能和服役可靠性。

工业系统通常部署可见光相机与红外热像仪同步监测 SLM 过程 \\cite{ref_multisensor_am,ref_multisensor_platform}。可见光成像捕捉熔池形态和表面纹理，红外热成像反映热输入分布和温度场演化。这种多模态观测提供互补的缺陷表征信息。然而，现有监测方法大多假设训练数据与测试数据来自相同分布：当工艺参数、设备型号或粉末批次变化时，模型识别性能显著下降。

这一失效模式源于深度学习模型对虚假相关性（spurious correlations）的过度依赖 \\cite{ref_irm,ref_groupdro}。模型倾向于利用背景纹理、光照条件或设备噪声等表面统计特征进行预测，而非缺陷本质的因果特征。在 SLM 多模态监测中，这意味着模型可能将特定工况下的热背景模式误识为缺陷依据，导致在未见工况（unseen condition）下失效。如何实现训练工况到未见工况的稳定识别，成为 SLM 缺陷识别的核心问题。

预训练视觉大模型为上述问题提供了可能的解决路径。这些模型在海量多模态数据上训练，具备处理异构视觉输入的能力；其接触到的多样化视觉模式，理论上应有助于提取跨工况稳定的特征。然而，现有实验表明：简单的提示工程（prompt engineering）方法在未见工况设定下性能不足。大模型的多模态先验并不能自动转化为对工业缺陷的准确识别，关键在于如何设计适配机制，使其在保持跨工况稳定性的同时，有效融合多源传感器数据中的互补信息。

本文提出一种基于大模型先验与因果稳定约束的未见工况缺陷识别方法。核心思路是将预训练视觉大模型作为跨工况特征提取器，通过显式多模态融合整合可见光与红外热信息，并在融合空间中施加因果一致性约束。该方法采用轻量化的判别式适配机制，在保留大模型预训练先验的同时，学习跨工况不变的缺陷表征。具体而言，"因果稳定"假设认为：与工况相关的纹理、亮度和热背景特征会随工艺条件变化，而缺陷本体的本质特征应当在跨工况时保持稳定。通过对同标签但来自不同工况的样本施加一致性约束，模型被迫减少对不稳定环境因素的依赖，从而学习到从训练工况到未见工况的稳健映射。

本文的主要贡献包括：（1）构建条件级主协议与外部迁移测试集，建立未见工况下模型性能的系统评估框架；（2）设计大模型先验引导的判别式适配方法，通过因果稳定约束增强未见工况识别性能；（3）分析可见光与红外模态在未见工况下的互补机制，探索多模态信息的最优融合策略。'''

if old_intro in content:
    content = content.replace(old_intro, new_intro)
    with open('Casual-Qwen_Paper_CN.tex', 'w', encoding='utf-8') as f:
        f.write(content)
    print("Replacement successful!")
else:
    print("Old intro not found")
    # 打印引言部分帮助调试
    start = content.find('\\section{引言}')
    end = content.find('\\section{相关工作}')
    if start != -1 and end != -1:
        print(f"Found intro section: {start} to {end}")
        print("First 300 chars:", repr(content[start:start+300]))
