# Paper Role Review

## 1. 通讯作者视角

- 论文题目与主线已经重新对齐为：
  - 中文：`基于大模型先验与因果稳定约束的SLM多模态缺陷识别方法`
  - 英文：`Large-Model Prior and Causal Consistency for Unseen-Condition Multimodal Defect Recognition in SLM`
- 当前英文稿和中文稿都把以下四个关键词置于论文主轴，而不是只停留在标题层面：
  - `多模态`
  - `未见工况`
  - `大模型先验`
  - `SLM 缺陷识别`
- 创新点已进一步收束为：
  - `因果稳定/一致性约束提升小样本大模型适配性能`
  - 而不是 `prompt 技巧`、`单纯换更大模型` 或 `真微调更强`
- 方法部分已经重写为清晰的四层结构：
  1. 多模态数据特征提取
  2. 因果稳定表征约束
  3. 大模型轻量适配与真微调对照
  4. 输出层、训练目标与伪代码

## 2. 领域审稿人视角

- 公式推导已与仓库实现严格对齐，不再停留于概念性描述：
  - 明确写出 Qwen 视觉 token 的平均池化
  - 明确写出跨工况同标签正样本池 $\Omega_i$
  - 明确写出带 `stop-gradient` 的因果一致性损失
  - 明确区分 `冻结式轻量适配` 与 `last2 true fine-tuning` 的可训练参数集合
- 已补入模型训练伪代码，解决原稿只有口头流程、没有算法化描述的问题。
- 关键实验叙事已经重新组织为：
  - `Prompt-Qwen` 失败，说明大模型先验不能直接通过 prompt 闭集分类转化为工业识别能力
  - `Frozen Qwen prior + explicit fusion` 有效，说明判别式适配是正确方向
  - `Causal consistency` 继续提供增益，支撑论文创新点
  - `Last2 true fine-tuning` 退化，解释为何激进微调在当前小样本设定下有害
- 多模态表述已做风险控制，避免把 IR 写成“无效模态”：
  - `IR-only` 结果仍具识别能力
  - 经典迁移最优方法使用 `triple-view late fusion`
  - 当前更准确的结论是：`IR 有效，但尚未在现有 Qwen 融合结构中被最优利用`

## 3. TII 编辑视角

- 相关工作已压缩为两大块，满足你提出的“不要写太多相关工作”的要求。
- 英文稿和中文稿都围绕 `7--8 页双栏` 的篇幅目标进行了压缩，避免把文章写成过长综述。
- 图、表、伪代码都已预留正式位置，并保留了必要的 `\label/\ref` 钩子：
  - 协议图
  - 方法框架图
  - 主结果图
  - 迁移结果图
  - 训练伪代码表
- 参考文献仍维持为已核验的主来源文献，没有回退到占位式引用。

## 4. 当前仍待补充的投稿前事项

- 补真实作者、单位、基金和通讯作者信息。
- 将占位图替换为最终导出的正式图片。
- 对英文稿和中文稿分别做一次实际编译检查：
  - 英文稿：`pdflatex + IEEEtran`
  - 中文稿：`XeLaTeX + ctex + IEEEtran`
- 若后续准备投稿前再做一轮强化，可优先检查三类细节：
  - 公式编号与正文引用是否完全一致
  - 伪代码与最终训练实现命名是否完全一致
  - 是否还需要补 1--2 篇更贴近 `工业大模型适配` 的近年参考文献
